-- These tables belong to an abandoned schema and were verified empty before
-- this migration was created. CASCADE is intentionally omitted so unexpected
-- dependencies stop the migration instead of being removed implicitly.
DROP TABLE IF EXISTS "StudentClass";
DROP TABLE IF EXISTS "classes";
DROP TABLE IF EXISTS "students";
